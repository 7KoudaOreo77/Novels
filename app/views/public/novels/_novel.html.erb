<%#= link_to image_tag( post.image.variant(resize: '100x100') ), post_path(post.id) %>
<%= link_to post.user.name, user_path(post.user.id) %>
<i class="fa fa-heart unlike-btn"></i>
<%= novel.likes.length %>
<%= link_to novel.title, public_novel_path(novel.id) %>